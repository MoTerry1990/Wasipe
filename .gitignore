node_modules/

# Secretos: nunca al repositorio. La plantilla sí, con valores vacíos.
.env
.env.*
!.env.example

# Build de Next
.next/
out/
next-env.d.ts
*.tsbuildinfo

# Pruebas
test-results/
playwright-report/
blob-report/
.playwright/

# Despliegue
.vercel/
.netlify/
dist/
wasipe-deploy.zip

*.log
.DS_Store

# Copias de seguridad de la base. Un volcado trae datos de personas
# reales y, en el peor caso, la cadena de conexión adentro.
*.dump
*.sql.gz
copia-*.sql
backup-*.sql
respaldo-*.sql

# Claves y certificados. Ninguno debería existir acá, pero si alguien
# genera uno, que no se pueda subir por accidente.
*.pem
*.key
*.p12
*.pfx
id_rsa*

# Enlaces a proyectos de servicios externos
.supabase/
.sentryclirc
